# משימות ל-Antigravity — מאת Claude (מתואם מול דור)

> עדכון מצב: דור ביטל את גיבוי OneDrive על התיקייה. במהלך הלילה OneDrive חתך 6 קבצים
> (game.js, ‏.gitignore, ‏style.css.map, ‏tests/e2e/gameplay.spec.js, ‏www/game.js, ‏www/style.css.map) —
> כולם שוחזרו מ-git ואומתו (15/15 בדיקות ירוקות). האתר החי ב-Netlify אומת בריא מבפנים:
> המשחק עולה, 4 הקונטרולרים פעילים, תיקון המשימות באוויר, ה-cache-buster האוטומטי שלך עובד.

## סדר עדיפויות

### 1. אימות ובנייה מחדש של ה-AAB (לפני העלאה לחנות!)
הקובץ `android\app\build\outputs\bundle\release\app-release.aab` מתוארך **17 ביולי** — לא מהבנייה
האחרונה שלך. סביר שהבנייה לא כתבה קובץ חדש למיקום הצפוי.
- [ ] הרץ `build_aab.ps1` מחדש (עכשיו כש-OneDrive כבוי)
- [ ] ודא שתאריך הקובץ התעדכן
- [ ] פתח את ה-AAB כ-zip וודא ש-`base/assets/public/game.js` שלם ועובר `node --check`
- [ ] רק אז — דור מעלה ל-Google Play Console

### 2. סגירת ה-working tree
יש ~60 קבצים משוננים לא-מקומטים (בעיקר שלך). לעבור עליהם ולקמט/לזרוק **במודע**.
- [ ] לפני כל קומיט: `npm test` + `npm run lint` + ‏`node --check` על כל קבצי הליבה
- [ ] **לא** `git add -A` עיוור — זה מה שערבב את phase 4 של Claude לתוך קומיט ה-UI שלך

### 3. חיסון ה-build נגד קבצים קטועים
הבאג המרכזי של היום: קובץ קטוע → build נכשל בשקט → נכסים ישנים נשלחים.
- [ ] סקריפט `tools/verify-integrity.js`: מריץ `node --check` על כל ‎.js בפרויקט (ללא node_modules/www)
      + בדיקת איזון סוגריים ב-scss + השוואת גודל מול git כשרלוונטי
- [ ] לחבר אותו כצעד ראשון ב-`npm run build` — כישלון עוצר את הבנייה **בקול רם**
- [ ] לוודא שה-cache-buster האוטומטי שלך מכסה את כל התגיות (style.css, app.bundle.js, וכל 14 הסקריפטים)
- [ ] להוסיף `.bat` לרשימת ההחרגות של build.js (שקובץ `הפעל-שרת.bat` לא ייארז ל-www/AAB)

### 4. בדיקת E2E לתצוגת המשימות (רגרסיה לבאג של היום)
בדיקות הלוגיקה לא תפסו את הבאג כי הוא היה ויזואלי. להוסיף ל-`tests/e2e`:
- [ ] השלמת משימה דרך `page.evaluate` (כמו שהבדיקות הקיימות עושות)
- [ ] פתיחת טאב משימות
- [ ] assert: ה-boundingBox של `.claim-reward-btn` כלול במלואו בתוך ה-boundingBox של `.mission-card` שלו
- [ ] assert: הסכום בכפתור == הסכום בתגית של אותו כרטיס

### 5. Backlog (לא דחוף)
- [ ] בדיקות יחידה ל-ShopController (זרימות קנייה) ולפרסטיז' מלא
- [ ] עדכון `REFACTOR_PLAN.md` — לסמן שלבים 1–4 כבוצעו
- [ ] deploy מחודש ל-Netlify אחרי שהכל מקומט (לוודא שהאתר תואם ל-HEAD)

## כללי עבודה (חשוב — סוכם עם דור)
1. **סוכן אחד בכל רגע על התיקייה.** לפני עבודה — לוודא ש-Claude לא באמצע, ולהפך.
2. אחרי כל פעולת קבצים גדולה — בדיקת שלמות (סעיף 3 יהפוך את זה לאוטומטי).
3. קומיטים ממוקדים עם הודעות ברורות, בלי לערבב עבודה של סוכן אחר.
